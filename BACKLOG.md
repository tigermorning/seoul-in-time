# 백로그 — 2026-09-12 기준

> **새 세션은 이 파일부터 읽는다.** 「지금 상태」가 인수인계, 「할 일」이 순서대로 할 것.
> 오늘 결정: B안(파노라마)이 영상 경험과 일치. 파노라마 스팟은 **남산 회현동 조망점**.

---

# 지금 상태

## 브랜치

| 브랜치 | 내용 |
|---|---|
| `master` | 기본 브랜치. 푸시하면 GitHub Pages 자동 배포 (약 1분) |
| `wip/photo-credit` | 출처표시 렌더 작업. master 합쳐져 있고 검사 통과. **머지 대기** |

`wip/photo-credit`은 아래 7번의 리뷰 #7을 구현한 것이다. `src/lib/credit.ts`(출처
문구 조립, 테스트 4개)와 `src/components/PhotoCredit.tsx`(안내 화면 출처 카드 +
A·B·C 정렬 화면 하단 접이식 출처줄)를 추가하고 네 화면에 붙였다. 검사 37개 통과.
**다음 세션이 판단할 것: 폰에서 한 번 보고 master에 머지할지.** 배포본이 공공누리
출처표시 의무를 어기고 있는 상태라 우선순위가 높다.

## 배포·환경

- 배포 https://tigermorning.github.io/seoul-in-time/ · 저장소 https://github.com/tigermorning/seoul-in-time
- `master` 푸시 → `.github/workflows/deploy.yml`이 `npm ci` → `npm run check` → 빌드 → Pages.
  검사가 깨지면 배포되지 않는다. Pages 배포 큐가 7분까지 걸린 적 있음
- **클라우드 세션(휴대폰)은 `.claude/hooks/session-start.sh`가 `npm ci`를 자동 실행**한다.
  바로 `npm run check` / `npm run build`를 돌리면 된다. 로컬 PC 세션에서는 훅이
  아무것도 하지 않는다 (이미 설치돼 있으므로)
- `archive-raw/`(파노라마 원본, gitignore)는 클라우드에 없다. 필요하면 다시 받는다:
  `pip install -r scripts/requirements.txt` → `python scripts/fetch_namsan_sources.py`

## 클라우드 세션에서 되는 일 / 안 되는 일

- **된다** — 코드·문서·스팟 JSON 수정, `npm run check`, 빌드, 파노라마 재생성(재다운로드 후), 배포
- **안 된다** — 폰 센서·카메라 실측. 아래 **A1·A2**가 여기 걸린다.
  방법은 있다: 배포 사이트를 폰 브라우저로 직접 열어 확인하고, 본 결과를 세션에
  불러주면 문서·코드에 반영한다. 세션이 대신 볼 수는 없다

## 최근 이력

- 09-10 정렬 화면 3종 동작 (A 카메라 오버레이 / B 파노라마 three.js / C 창 모드). 실험 기록 방식별 집계
- 09-10 폰 실기기: B 데모 격자에서 나침반·피치·롤 반영 확인 (일산). **절대 방위 정확도는 미확인**
- 09-11 남산 파노라마 1910·1925·1974 equirect 합성 완료 → [PANORAMA_SOURCES.md](PANORAMA_SOURCES.md)
- 09-11 독립 검토 [REVIEW-2026-09-11.md](REVIEW-2026-09-11.md). 판정 **REVISE**
- 09-12 클라우드 세션용 SessionStart 훅 추가 (master)
- 09-12 출처표시 렌더 구현 (`wip/photo-credit`, 머지 대기)

---

# 할 일

09-11 검토가 순서를 바꿨다. **B1(Phase 0 판정 규칙)과 B2(출처표시 머지)가
A2(남산 폰 확인)보다 앞선다.** 시대 탭과 8192 폭은 폰 검증 전까지 중단.

## 폰이 있어야 하는 것 (현장/실기기)

### A1. 절대 방위 검증 (10분, 어디서든)
- 왜: 뷰어 부호가 맞는지 실측 없이는 모름. 리뷰 #1 (iOS 편각) 미해결
- 무엇: 나침반 앱으로 북쪽 보고 `[데모] 파노라마` 열기. 중앙에 `N 0°` 오는지.
  어긋나면 ⚙ 편각 토글 후 재확인
- 완료: REVIEW-2026-09-10.md #1에 "iOS webkitCompassHeading = 진북/자북" 확정 기록
- 같이: `Trial`에 편각 적용 여부 필드 추가 (검토 B2)

### A2. 남산 파노라마 폰 확인
- 왜: 합성은 끝났고 실기기 확인만 남음. 이게 되면 영상 경험 재현
- 무엇: 홈 `남산 회현동 조망점` → B. 북쪽(346°)에 북악·경복궁 일대, 7°에 명동성당이 오는지
- 데스크톱 오프라인 렌더로는 확인됨. 수치·근거는 PANORAMA_SOURCES.md 「2026-09-11」 절
- 역산한 촬영 지점은 백범광장이 아니라 약 500m 동쪽 케이블카 선로 부근(37.5541, 126.9857).
  **답사 대상이 바뀌었다**
- 해상도: 4096 폭에선 1910이 흐림. 폰으로 보고 `--width 8192` 여부 결정
- 완료: 폰 B 모드에서 북쪽 보면 1910년 경복궁·종로가 보인다

### A3. 현장 실험 기록
- 청계천 광교에서 B·C·A 순서로 ([BENCHMARK_CITY_IN_TIME.md §7.3](BENCHMARK_CITY_IN_TIME.md))
- 폰 센서 체크리스트 5개는 README 「폰 센서 체크리스트」 표

## 클라우드 세션에서 되는 것

### B1. Phase 0 판정 규칙 (검토가 최우선으로 올림)
- 왜: A/B/C 중 무엇으로 갈지 고르는 기준이 없다. 기준 없이 실험해도 결론이 안 난다
- 무엇: PRD §6을 2조건 표로, TEST_PROTOCOL의 B/C 문구 정리, MVP §3·§4·§6 피벗 반영
- 완료: "성공률 N% 이상이면 X안" 형태로 문서에 박힘

### B2. 출처표시 머지 (배포본 위반 상태)
- 왜: 배포된 사이트가 공공누리 1유형 출처표시 의무를 지키지 않고 있다
- 무엇: `wip/photo-credit` 폰에서 확인 후 master 머지. 구현은 끝나 있음
- 남은 것: `license.determination` 필드 (리뷰 09-10 #6)는 별도
- 완료: 배포본 안내 화면에 출처 카드, 정렬 화면에 출처줄

### B3. 합성 표기 규칙
- 왜: 없는 하늘을 채워 넣은 파노라마를 "옛 사진"이라 부르면 안 된다
- 무엇: 합성 허용 범위·표기 규칙 문서화 + PanoScreen 안내문 수정 (검토 A3)

### B4. 데모 격자 처리
- 왜: 실제 스팟이 생겼으니 격자는 개발용. 일반 사용자 목록에 남으면 혼란
- 무엇: `status: "draft"`인 스팟을 홈에서 숨기는 토글, 또는 `?dev=1`로만 표시
- 완료: 배포 URL 홈에 격자 안 보임, 개발 시엔 접근 가능

### B5. 시대 선택 (B 모드) — **A2 끝나고**
- 왜: 남산 스팟에 시대가 3개. City in Time엔 없고 우리만 되는 것
- 무엇: PanoScreen에 시대 탭. 지금은 equirect가 여럿이면 첫 장만 그린다(겹치면 z-fighting)
- 완료: 1910 ↔ 1925 ↔ 1974 전환

### B6. 딥링크 `#/spot/{id}`
- 왜: QR·SNS 공유 둘 다 전제 (BENCHMARK §4.2). 지금은 URL이 항상 홈
- 무엇: 해시 라우팅. `#/spot/namsan-hoehyeon/pano` 바로 진입. `?m=1` → trial에 marker 태그

### B7. 공유 화면 (F5)
- A: 합성 캡처. B/C: 파노라마 캡처 + 현재 카메라 사진 나란히 (영상의 유튜버 편집 = 이 기능)
- Web Share. **출처 문구를 이미지에 굽는 것**(리뷰 #7 나머지)이 여기 포함

### B8. 문서 반영 (BENCHMARK §8 미적용 목록)
- PRD §5 F1에 실물 마커 채널 / §8.2·§10 / §8.3 "28개"→46개
- PRD 비목표 5에 위촉 일러스트 허용 여부 명시
- MVP §5.4 / TEST_PROTOCOL — A·B·C 세 방식 번갈아 기록 절차, 임시 마커 A4 사양
- PLATFORM_COMPARISON — 정렬 방식 3안 비교 절

### B9. 기존 리뷰 잔여 (REVIEW-2026-09-10.md)
- #5 광화문 스팟 1건 (서울역사아카이브) — 남산 하고 나서
- #6 `license.determination` 필드 / ~~#7 attribution 렌더~~(09-12, B2에서 머지 대기) /
  #8 crop 적용 / #9 A 모드 배지 ±4°→±10° / #12 `cancel-in-progress: false` / #14 검증기 규칙

## 보류 (건드리지 않음)

- 국립중앙박물관 유리건판 수동 검색 (1유형 확인됨, 항목 미확인)
- 시대 음향 (City in Time은 코어지만 우리 P2)
- 셀피 스팟
- 파노라마 스팟 실물 마커 — Phase 1
- 홍콩 사이트 재조사 — [BENCHMARK §9](BENCHMARK_CITY_IN_TIME.md). 화면 안 와이프 슬라이더가 C안 완성형

## 메모

- 아카이브 검색어는 "파노라마" 아니라 **"전경" / "N매 연속"**
- 5173은 다른 node 프로세스가 https로 점유 중이었음. `.claude/launch.json`의 `dev-http-5174`가 우회. 정리해도 됨
- 빌드 25초. Pages 배포 큐가 7분까지 걸린 적 있음
- 로컬 npm이 샌드박스보다 높은 버전이라 `npm install`은 락파일의 `libc` 필드를 지운다.
  훅이 `npm ci`를 쓰는 이유. 락파일이 이유 없이 변경돼 있으면 되돌릴 것
