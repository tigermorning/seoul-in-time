# 벤치마킹 — City in Time 실물 사인

> 2026-09-10 · 근거: 홍콩 현지 촬영 사진 2장 (彌敦道/Nathan Road, 油麻地－佐敦 구간)
> 상위 문서: [PRD.md](PRD.md) · [MVP.md](MVP.md) · [TEST_PROTOCOL.md](TEST_PROTOCOL.md)
>
> **한 줄 결론:** City in Time은 우리가 소프트웨어로 풀려는 문제(GPS 오차·방위
> 오차·스팟 발견)를 **거리에 세운 실물 기둥으로 우회**했다. 우리 PRD·MVP에는 이
> 물리 층이 아예 없다. Phase 0 판정 기준이 이 차이를 구분하지 못하면 **틀린 결론**
> 이 나온다.

---

## 1. 관찰 — 실물 사인 해부

빨간 기둥 하나. 위에서 아래로 **3층**, 각 층이 서로 다른 질문에 답한다.

| 층 | 내용 | 답하는 질문 |
|---|---|---|
| **상단 원반** | 흰 원 안에 빨간 QR 코드. 사람 가슴~눈높이 | *어떻게 보나* |
| **중단 기둥** | 폰 든 손 픽토그램 + `掃描二維碼觀賞城市景昔 / Scan the QR code to see CITY IN TIME` | *이게 뭐 하는 물건인가* |
| **하단 원반** | `CA. 1950` (대형) · `彌敦道 / Nathan Road` · `油麻地－佐敦 / Yau Ma Tei-Jordan` · **흑백 옛 사진 2장 좌우 분할** · 푸터에 Tourism Commission + CityU 로고 + `www.cityintime.hk` | *왜 폰을 꺼내야 하나* |

### 관찰에서 바로 읽히는 설계 결정 8개

1. **기둥 자체가 촬영 지점이다.** 좌표를 안내하는 물건이 아니라, 좌표 **그 자체**.
   사용자는 "기둥 앞에 서라" 한 문장이면 된다. GPS 정확도가 경험에서 빠진다.
2. **기둥 방향이 방위를 준다.** 사인 면이 향한 쪽 = 옛 사진이 바라보던 쪽.
   나침반이 틀려도 사람은 판을 보고 몸을 돌린다.
3. ~~**앱스토어 없음, QR 하나.** 웹 우선.~~ **09-11 정정(§9.0): 네이티브 앱이 본체, 웹은 폴백.** 우리 PWA 선택과는 다른 판단
   ([PLATFORM_COMPARISON.md](PLATFORM_COMPARISON.md)).
4. **스캔 전에 이미 값을 준다.** 연도 + 지명 + 옛 사진 2장. 폰을 안 꺼낸 사람도
   "여기가 1950년엔 저랬구나"를 얻는다. 앱은 **증폭**이지 전부가 아니다.
5. **옛 사진을 좌우 분할로 두 장** 배치 — 앱의 대조 뷰(F3)를 실물에서 예고.
6. **`CA.`(circa)를 대놓고 쓴다.** 연도 불확실을 숨기지 않음. 아카이브 신뢰 방식.
7. **중문·영문 병기.** 다국어를 앱이 아니라 **인쇄물**로 먼저 해결.
8. **정부기관 + 대학 공동 로고 + URL 인쇄.** 정당성 확보 + QR 못 읽는 사람의 폴백.
9. **설치 위치가 횡단보도 대기 공간.** 이미 멈춰 서서 주변을 두리번거리는 사람이
   있는 자리. 지나가는 사람이 아니라 **서 있는 사람**을 노린다.

---

## 2. 우리 문서와의 격차

| 우리 상태 | City in Time | 결과 |
|---|---|---|
| PRD §5 F1 스팟 발견 = **지도 + 목록**뿐 | 지도 + **거리 실물** | 우리는 "앱을 이미 아는 사람"만 도달. PRD §2.2가 지적한 "찾아 나서는 사람만 도달"을 우리가 반복 |
| PRD §8.2 GPS 오차 10~30m → *"육안 랜드마크로 안내"* | 오차를 **없앰** (기둥에 서면 끝) | 우리 대응은 완화, 저쪽은 제거 |
| 나침반 오차 ±15° → 수동 미세조정 UI | 판 방향이 물리적 기준 | 우리는 오차를 사용자에게 떠넘김 |
| F6 다국어 = P1 (앱 i18n) | 인쇄 병기 | 인쇄가 훨씬 쌈 |
| 리뷰 #7 — 출처표시가 앱에 안 뜸 | 로고·URL을 판에 인쇄 | 물리 층에도 출처 자리가 있다 |
| 앱 라우팅 없음 (`App.tsx`는 `useState` 화면 전환) | QR = 스팟 딥링크 | **QR도 SNS 공유(F5·F10)도 딥링크가 전제.** 지금은 둘 다 불가 |

---

## 3. 가장 중요한 지점 — Phase 0 판정 기준이 오염돼 있다

PRD §6 Phase 0: *정렬 성공률 70% 미달이면 플랫폼 재검토(웹 → AR 네이티브)*.

이 판정은 **"실물 마커 없음"을 암묵 전제**로 한다. 그런데 실패 원인은 최소 셋이다:

```
정렬 실패
├── (A) 사용자가 엉뚱한 자리에 섰다        ← 마커가 푸는 문제
├── (B) 사용자가 엉뚱한 방향을 봤다        ← 마커가 푸는 문제
└── (C) 자리·방향은 맞는데 화면이 안 맞았다 ← 플랫폼(웹 센서)이 푸는 문제
```

현재 `src/lib/trials.ts`는 셋을 구분하지 않는다. 성공률 45%가 나왔을 때
**(A)(B) 때문인데 (C)로 오독하면 네이티브 AR로 갈아엎는다** — MVP.md §5가
스스로 "최악 시나리오"라 부른 그 경로. 반대로 (C)가 진짜 원인인데 마커 탓으로
돌려도 똑같이 틀린다.

### 조치 — Phase 0 프로토콜을 2조건으로 쪼갠다

같은 스팟에서 참가자를 두 조건에 나눈다 (또는 순서 교차):

| 조건 | 절차 | 측정하는 것 |
|---|---|---|
| **A. 마커 없음** | 앱만 주고 지도·안내문으로 찾아가게 함 | 현재 PRD 기준 그대로 |
| **B. 마커 있음** | 진행자가 임시 마커를 바닥에 두고 "여기 서서 이 판이 보는 쪽" | (C)만 남긴 순수 정렬 성능 |

판정 표를 이렇게 다시 쓴다:

| B 성공률 | A 성공률 | 결론 |
|---|---|---|
| 70%+ | 70%+ | 통과. 마커는 선택 |
| **70%+** | **낮음** | **웹은 문제없음. 실물 마커가 제품의 전제** → Phase 1 로드맵에 마커 설치 편입. 플랫폼 교체 아님 |
| 낮음 | — | 진짜 플랫폼 문제. 네이티브 AR 재검토 |

> 이 표가 없으면 Phase 0는 **판정을 못 한다.** 리뷰 #4로 계측 수단은 생겼지만,
> 계측된 숫자의 **해석 규칙**이 아직 없다.

---

## 4. Phase 0에서 지금 당장 되는 것 (허가·예산 0)

실물 기둥은 도로점용허가가 필요하지만, **테스트용 임시 마커는 필요 없다** —
진행자가 들고 가서 테스트 중에만 놓고 회수한다.

### 4.1 임시 마커 사양 (A4 코팅 1장 + 바닥 테이프)

City in Time 3층 구조를 A4에 그대로 압축:

```
┌─────────────────────────┐
│   [QR]  ← 딥링크         │  상단: 스캔
│  스캔해서 지금 보기        │
├─────────────────────────┤
│  CA. 1964  청계천 광교     │  중단: 왜 (연도 + 지명 ko/en)
│  Cheonggyecheon Gwanggyo │
├─────────────────────────┤
│  [옛 사진]  [옛 사진]      │  하단: 미리보기 2장 좌우 분할
├─────────────────────────┤
│   ▲ 이쪽을 보세요          │  방위 화살표 = 판을 놓는 방향이 곧 기준
│  서울기록원 · 공공누리 제1유형│  푸터: 출처 (리뷰 #7의 물리판)
└─────────────────────────┘
```
바닥에 발 위치 테이프 2줄. 이게 좌표·방위·화각 기준점 전부를 고정한다.

### 4.2 딥링크 라우팅 (QR·SNS 공유 공통 전제)

`App.tsx`가 `useState<Screen>`으로만 화면을 바꾼다 → URL이 항상 같다. QR도
공유 링크도 홈으로만 떨어진다. PRD §5는 F10(원격 모드)을 *"트래픽의 대부분이
될 가능성"* 이라 적어놓고 그 링크를 만들 수단이 없는 상태.

최소 형태 — 해시 라우트 (GitHub Pages에서 서버 설정 불필요):

```
#/spot/cheonggyecheon-gwanggyo          → 안내 화면
#/spot/cheonggyecheon-gwanggyo/align    → 정렬 화면 바로
#/spot/cheonggyecheon-gwanggyo?m=1      → 마커 경유 진입 (조건 B 자동 태깅)
```

`?m=1`을 `trials.ts` 레코드에 그대로 실으면 §3의 A/B 구분이 **수기 기록 없이**
자동으로 잡힌다.

### 4.3 스키마 추가 2개

```jsonc
// historicalPhoto
"year_precision": { "enum": ["exact", "circa", "decade"] }  // CA. 표기 근거

// spot 최상위
"marker": {
  "installed": false,           // 실물 설치 여부
  "kind": "temporary",          // temporary | permanent | host-facility
  "bearing_deg": 0,             // 판 정면이 향하는 방위 = viewpoint.heading_deg 와 정합 검증 대상
  "note": ""
}
```
`marker.bearing_deg` ↔ `viewpoint.heading_deg` 정합은 `validate-spots`에
규칙으로 넣는다 (리뷰 #14와 같은 자리).

---

## 5. Phase 1 — 실물 마커 설치 경로

값싼 순서대로. 전부 기다릴 필요 없다.

| 방식 | 허가 | 비용 | 비고 |
|---|---|---|---|
| **협력 상가·카페 앞 (사유지)** | 점포주 동의만 | 최저 | 스팟 옆 가게 1곳 섭외. Phase 1 초반 현실안 |
| **기존 시설물 부착** — 관광안내표지판, 버스정류장, 문화재 안내판 | 시설 관리주체 협의 | 낮음 | 서울시 관광안내표지 정비사업과 붙이면 명분 생김 |
| **독립 기둥 신설** | 도로점용허가(도로법) + 옥외광고물 검토, 관할 구청 | 높음 | City in Time과 동형. 공공 지원사업 선정 후가 현실적 |

PRD §8.1은 *"공공 지원사업 선정 시 기관 협의가 수월"* 이라고 사진 라이선스에
대해 적었다. **마커 설치 허가도 같은 협의 테이블에 올린다** — 오히려 지자체
입장에서 사진 개방보다 마커 설치가 더 내주기 쉬운 항목이다(가시적 성과물).

동시에 이건 **공모 신청서의 차별점**이기도 하다. "앱 하나"가 아니라
"거리 위 설치물 + 앱" 조합은 관광·도시재생 계열 공모에서 점수가 다르다.

---

## 6. 채택하지 않는 것

| City in Time | 판단 |
|---|---|
| 360도 파노라마 재구성 (화가가 그림) | 스팟당 비용 수십 배. [MVP.md §3](MVP.md) 기결정 유지 |
| 정부·대학 공동 브랜딩을 **먼저** 확보 | 그거 기다리면 Phase 0가 멈춘다. 임시 마커로 선진행 |
| 전용 앱 다운로드 유도 | 우리는 웹 PWA — QR이 곧 앱 |
| 마커 없이는 못 쓰는 설계 | 우리는 **F10 원격 모드**가 있다. 마커는 현장 경험을 **강화**하는 층이지 필수 층이 아니어야 함 (SNS 확산이 P2 성장 엔진이므로) |

---

## 7. 실제 구현 해부 — cityintime.hk 직접 조사

> 2026-09-10 · 사인의 QR이 가리키는 대상을 브라우저로 직접 열어 확인.
> 사진 속 사인은 **`ymt-jd-004` (Nathan Road ca.1950)** — QR 목적지는
> `https://www.cityintime.hk/en/spot/ymt-jd-004/`.
>
> **저작권 경계:** 자산 URL이 공개돼 있지만 저작권자는 Tourism Commission ·
> CityU다. **구조·수치만 참고하고 자산은 가져오지 않는다.** 아래는 전부
> "그들이 어떻게 만들었나"의 관찰 기록.

### 7.1 스팟 목록이 공개 JSON API다

```
GET https://www.cityintime.hk/?www_api=1&post_type=spot
→ { code: 200, spotlist: [ … ] }
```

레코드 1개:
```json
{
  "name": "Nathan Road ca.1950", "id": "ymt-jd-004",
  "lat": 22.3082791, "lng": 114.1716521,
  "district": "Yau Ma Tei - Jordan",
  "thumbnails": "…-800x400.jpg",
  "pattern": "…MapPattern_YMT…-800x62.webp",
  "hidden_spot": false,
  "artist": "Artwork by Carmen Ng",
  "url": "https://www.cityintime.hk/en/spot/ymt-jd-004/",
  "label": "Visit Location"
}
```

**필드가 이것뿐이다.** `heading_deg`도 `hfov_deg`도 `pitch`도 없다 — 이유는 §7.3.

### 7.2 실측 규모 (PRD §8.3의 "28개 스팟"은 낡은 숫자)

| 항목 | 실측 |
|---|---|
| 스팟 총계 | **46개** (`hidden_spot: true` 2개 포함) |
| 지구 | 8곳 — Central 8, Kowloon City 7, Lei Yue Mun 6, Sham Shui Po 6, Tsim Sha Tsui 6, Yau Ma Tei-Jordan 6, Tai Hang 5, The Peak 2 |
| 연도 범위 | **1860 ~ 1987** |
| 제작 방식 | **일러스트레이터 위촉 26개** (실명 크레딧 14명) / 사진 기반 20개 |
| 부가 축 | 지구별 eBook PDF, 영화 촬영지 핫스팟(Movie Hotspots) |

- **지구 단위로 순차 출시**했다. Central(2020) → … → Kowloon City(2025).
  스팟 3개가 아니라 **지구 1개**가 그들의 출하 단위.
- `hidden_spot` — 같은 좌표에 `004a`/`004b`(Side B). 한 지점에서 **두 방향**을
  보게 하고, 두 번째는 목록에 숨겨 현장에서만 발견되게 한다.
- **절반 이상이 사진이 아니라 위촉 일러스트다.** 사진이 없는 시대·지점을
  화가로 메운다. 우리 PRD 비목표 5는 *AI 생성·사진 변형*을 금지하지만
  **사람이 그린 위촉 일러스트는 다른 범주**다. 우리 문서에 이 선택지가 없다.

### 7.3 핵심 발견 — 그들의 "AR"에는 카메라가 없다

`assets/js/babylonScene.js` (Babylon.js 4.2):

```js
if (hasDeviceOrientation) {
  camera = new BABYLON.DeviceOrientationCamera("camera1", …, scene);
} else {
  camera = new BABYLON.FreeCamera("camera1", …, scene);   // 드래그로 둘러보기
}
```

`getUserMedia` 호출이 없다. 라이브 카메라 영상 위에 옛 사진을 겹치는 게 아니라,
**360도 파노라마 안에 사용자를 넣고 폰을 창처럼 쓴다.** 현재 풍경과의 대조는
~~화면이 아니라 **사용자가 폰에서 눈을 떼는 순간** 일어난다.~~ **09-11 정정(§9.0): 화면 안 세로 와이프 슬라이더로 과거/현재를 대조한다.**

이 한 가지 결정이 우리 리뷰 목록의 절반을 삭제한다:

| 우리 문제 | 그들에게는 |
|---|---|
| 리뷰 #10 iOS 카메라·나침반 권한 체인이 `await`로 끊김 | **없음** — 권한이 나침반 하나뿐 |
| 리뷰 #11 `object-cover`가 화각 전제를 깸 | **없음** — 라이브 영상이 없음 |
| 리뷰 #8 `crop`/`hfov` 기준·광축 위치 미정의 | **없음** — equirect는 화각 개념이 없음 |
| `viewpoint.heading_deg` 추정 비용 | **없음** — equirect 이미지의 가로 오프셋 자체가 방위 기준. 저작 단계에서 이미지를 돌려 "픽셀 0열 = 북"으로 맞추면 끝 |
| 카카오톡·인스타 인앱 브라우저가 카메라 차단 (README 기록) | **영향 없음** |
| 리뷰 #1 iOS 편각 이중 보정 | Babylon `DeviceOrientationCamera`가 내부에서 처리 — 오일러 분해를 손으로 안 씀 |
| F10 원격 모드를 별도 화면으로 설계 | **같은 장면, 카메라만 교체** (`FreeCamera` 폴백). 화면이 하나 |

> **우리가 지금 고치고 있는 버그 다수는 "라이브 카메라 위 오버레이"를 골랐기
> 때문에 생긴 것이다.** 플랫폼(웹) 문제가 아니라 **정렬 방식 선택**의 문제다.
> Phase 0 판정에서 "웹은 안 된다"로 결론내기 전에 이 축을 먼저 검토해야 한다.

#### 정렬 방식 3안 비교 (새 결정 지점)

| 안 | 화면에 뜨는 것 | 카메라 권한 | 스팟당 콘텐츠 원가 | 상태 |
|---|---|---|---|---|
| **A. 라이브 영상 + 사진 오버레이** | 현재(영상) + 과거(반투명) | 필요 | 낮음 (사진 1장 + 지점·방위·화각 역산) | **현재 구현** |
| **B. 과거만 화면, 현재는 눈으로** (City in Time) | 과거 파노라마만 | **불필요** | 매우 높음 (파노라마 재구성) | 미검토 |
| **C. 과거만 화면 + 단일 사진** | 과거 사진 1장을 방위에 맞춰 띄움 | **불필요** | A와 동일 | **미검토 — 가장 싼 개선** |

C안이 문서에 없다. A의 콘텐츠 원가에 B의 권한·화각 면역을 합친 조합이다.
**임시 마커(§4.1)와 붙이면 특히 강하다** — 마커가 지점·방위를 고정하니
화면은 옛 사진만 띄우면 된다. Phase 0 조건 B에 C안을 같이 태우면
카메라 오버레이 없이도 "정렬의 순간"이 성립하는지를 같은 테스트에서 잰다.

### 7.4 스팟 1개의 자산 구성 (`ymt-jd-004` = 내부 ID `L019`/`P019`)

`<head>`에 `rel="preload" as="fetch"` / `rel="prefetch" as="image"`로 선언.

| 자산 | 크기 | 해상도 | 역할 |
|---|---|---|---|
| `L019-…_ca.1950.webp` | **2,449 KB** | **4096×2048** | 배포용 equirect 파노라마 (2:1) |
| `0_019_13K_fix.png` | **25,551 KB** | 13K급 | **제작 마스터가 공개 URL에 그대로 노출** |
| `SKY_MASK-8.png` | 154 KB | 4096×2048 | **하늘 분리 마스크** — 하늘만 따로 애니메이션 |
| `Web_L019_PoHing_mini-1.glb` | 1,611 KB | — | 건물 3D 모델 (`_mini` = 웹 경량판) |
| `L019MMC.wav` | 1,721 KB | — | 시대 음향. **무압축 WAV** (mp3/opus면 1/10) |
| `P019_L01_B01~B03.jpg` | 448·498·506 KB | 각 1024×768 | 원본 옛 사진 3장 — 슬라이드 대조용 |

**스팟당 배포 페이로드 ≈ 7.3 MB** (마스터 제외). 모바일에서 이걸 받는다.
우리 단일 사진 방식은 1~2 MB — 페이로드 여유가 **5배** 있다.

배울 점 / 안 배울 점:
- ✅ equirect **2:1 4096×2048 webp**가 그들의 배포 규격. 파노라마를 언제든 하려면 이 숫자가 기준선
- ✅ 하늘 마스크 분리 — 정적 사진에 움직임을 주는 최소 비용 기법
- ✅ `preload as=fetch` / `prefetch as=image`로 스팟 자산 선행 로드
- ❌ **13K 마스터 25MB를 공개 경로에 두면 안 된다** — 대역폭. ~~원본 유출~~ (09-11 정정: 유출이 아니라 3D 씬의 정식 배경 플레이트. §9.0)
  우리는 마스터를 저장소 밖(`archive-raw/`는 이미 gitignore)에 둔다
- ❌ 음향을 WAV로 내보내지 않는다 — opus/mp3

### 7.5 코드에서 확인된 기능 목록

`babylonScene.js` 함수: `initImageScene` / `initModelScene` / `syncCameras` /
`createHotspots` / `createSelfieSpot` · `createSelfiePlanes` / `initWebAudio` ·
`loadAudioBuffer` · `getAnimationAudioOffset` · `playSynchronizedAudio` /
`SpriteSheetAnimation` · `applyScalingToSpritesheetFrames`

- **2개 장면 합성.** `imgScene`(파노라마) + `modelScene`(glb)을 따로 렌더하고
  `syncCameras()`가 `onViewMatrixChangedObservable`로 두 카메라 시점을 미러링.
  `camControl` 플래그가 어느 쪽이 주도하는지 결정. 과거 건물만 3D로 세운다.
- **스프라이트시트 애니메이션 + 오디오 동기화.** `getAnimationAudioOffset()`로
  애니메이션 프레임과 음향 위치를 맞춤. 파노라마 위 사람·전차가 움직이고
  소리가 붙는다. 우리 F7(시대 음향)은 P2인데 **그들은 코어**다.
- **셀피 스팟** (`createSelfieSpot`) — 파노라마 안 지정 위치에서 사진을 찍게 하는
  전용 기능. 우리 F5(공유)는 "합성 이미지 생성"까지인데, 그들은 **어디서 찍을지까지
  지정**한다. P2(공유 = 성장 엔진) 관점에서 우리 F5보다 한 단계 위.
- **`initSlideComparison`은 라이브러리 없는 jQuery 40줄.** `.comparingItem`을
  `width: 50%`로 두고 핸들 `div`를 삽입, `mousedown`/`touchstart` 양쪽 바인딩.
  우리 F3도 이 정도면 충분하다는 근거.

### 7.6 즉시 옮겨올 것 3개

1. **스팟 ID 체계** — `{지구}-{번호}{a|b}`. 우리 `cheonggyecheon-gwanggyo`는
   슬러그가 길고 같은 지점의 다중 방향을 표현할 수 없다.
   → `jgn-001` / `jgn-001b` 형태 검토. 마커 인쇄면에도 짧은 ID가 낫다.
2. **출하 단위를 "지구"로.** 스팟 3개가 아니라 "청계천·광교 일대 N개"처럼 묶어야
   도보 동선(F9)·eBook·마커 설치 협의가 한 번에 굴러간다. Phase 1 스팟 30곳을
   **서울 6개 지구 × 5개**로 재편하면 로드맵이 지구 단위로 쪼개진다.
3. **`hidden_spot` 패턴.** 목록에 없고 현장에서만 발견되는 스팟.
   재방문·순회 동기(P1 잔존 엔진)를 공짜로 만든다. 필드 1개.

---

## 8. 문서 반영 목록 (아직 미적용)

- [ ] PRD §5 — F1에 "실물 마커" 채널 추가, 또는 F11 신설 (P1)
- [ ] PRD §6 Phase 0 — 판정 기준을 §3의 2조건 표로 교체
- [ ] PRD §8.2 — GPS·나침반 대응란에 "실물 마커" 추가
- [ ] PRD §10 리스크 — "마커 설치 허가 불발" 추가
- [ ] MVP §5.4 — 현장 테스트를 A/B 2조건으로 명시
- [ ] TEST_PROTOCOL — 임시 마커 사양 + 조건 배정 절차
- [ ] SPOT_SCHEMA — `year_precision`, `marker` 블록, `hidden_spot`, `district`, `artist`
- [ ] 코드 — 해시 라우팅, `?m=1` → trial 태깅
- [ ] **PRD §8.3 — "28개 스팟" → 실측 46개 / 8지구 / 1860~1987로 정정**
- [ ] **PRD 비목표 5 — "위촉 일러스트"가 금지 대상인지 명시** (그들은 26/46이 위촉 일러스트)
- [ ] **PLATFORM_COMPARISON — 정렬 방식 3안(§7.3) 비교 절 추가. C안이 문서에 없음**
- [x] **코드 — B안 프로토타입 `PanoScreen` + `panoScene.ts` (three.js).** 스키마에 `image.projection`(flat|equirect), `historical[].view`(사진별 방위·피치·화각) 추가. 데모 스팟 `demo-pano` = 방위 격자 테스트 패턴
- [x] **코드 — C안 프로토타입 `WindowScreen`.** 안내 화면에서 A/C 선택, 트라이얼에 `mode` 기록, 홈에서 방식별 집계
- [ ] **MVP §5.4 / TEST_PROTOCOL — Phase 0에 C안(카메라 없는 단일 사진)을 같이 태워 측정**
- [ ] Phase 1 출하 단위를 스팟 개수 → **지구**로 재편 (30곳 = 6지구 × 5)

---

## 9. 재조사 — 2026-09-11, 브라우저로 전체 플로우 실행

> 에이전트가 `ymt-jd-004`·`klc-002` 플로우를 직접 실행하고 Babylon 씬 객체·네트워크·JS 소스를
> 런타임에서 덤프. **[검증]** = 직접 확인 / **[추정]** = 정황 추론. §7과 겹치는 내용은 뺐다.

### 9.0 §1·§7의 오류 3개 — 본문에 취소선으로 정정

| 기존 서술 | 실제 | 근거 |
|---|---|---|
| §1-3 "앱스토어 없음, QR 하나. 웹 우선" | **틀림.** 네이티브 앱이 본체. iOS `id1529072870` / Android `hk.edu.cityu.cityintime`. 홈 진입 시 앱 설치 모달이 먼저 뜬다. FAQ: iOS/Android를 못 쓰는 기기는 "AR Clock의 QR을 스캔해 웹 기반 버전에 접속" — **웹은 폴백** | [검증] |
| §7.3 "대조는 폰에서 눈을 떼는 순간" | **틀림.** 화면 안 **세로 와이프 슬라이더**. User Guide: "Shift the slider to compare past and present views" | [검증] |
| §7.4 "13K 마스터 25MB 노출" | **오독.** `0_019_13K_fix.png`(7680×3840)는 모델 씬의 과거 배경 플레이트(`Mat_BG`) = 정식 배포 자산 | [검증] |

### 9.1 정보 구조

- [검증] 언어 3종 `/en/` `/tc/` `/sc/`, `hreflang` 정상. 스팟 URL `/{lang}/spot/{district}-{nnn}[a|b]/`. AR Clock QR = 이 URL 그대로.
- [검증] 발견 경로 **둘뿐**: `select-locations/`의 p5.js 추상 해안선 지도(실제 베이스맵 아님) + 지구별 드롭다운. **검색·내 주변·시대 필터 없음.** 목록 노출 44개(숨김 2 제외).
- [검증] 물리 마커 공식 명칭 **"AR Clock"**, 44개 설치. 영화 스팟엔 movie 아이콘.
- [검증] `data.gov.hk` / CSDI에 **"City in Time 프로젝트 위치" 오픈데이터셋**(문화체육관광국 제공) 등록. 정부 사업이 스팟 좌표를 공공데이터로 내는 패턴.

### 9.2 스팟 페이지

- [검증] 스팟 페이지에 **본문 텍스트가 없다.** `User Guide` / `Start` 버튼뿐인 게이트. 연도·설명·출처 미노출. 제목 배지(`Nathan Road ca.1950`)가 전부.
- [검증] 콘텐츠는 전부 3D 씬 **안의 핫스팟**. `ymt-jd-004`는 핫스팟 3개. 탭하면 모달: 제목 + 아카이브 사진 1장 + 약 300단어 서술 + 출처 1줄 + `Learn more`. `klc-002`는 아카이브 사진 5장.
- [검증] 스팟당 시대 **1개**(`ca.YYYY`). 다중 시대 없음. 같은 좌표 2방향은 `004a/004b`로 분리. 46개 / 8지구 / 1860~1987 — §7.2 수치 재확인. 위촉 일러스트 26 / 사진 기반 20, 일러스트레이터 실명 13명(§7.2 "14명"은 문자열 오카운트).
- [검증] `hidden_spot: true`는 `klc-004b` `klc-005b` 둘뿐, **2025년 九龍城 지구에서 처음 도입.** 구형 9개는 `null`(후행 추가 흔적).

### 9.3 파노라마 / "AR" 모드 — §7.3 보강

- [검증] **Babylon 엔진 2개 / 캔버스 2개** 동시 구동.

| 캔버스 | 내용 | 자산 |
|---|---|---|
| `panorama-image` | **현재** 360 사진. `PhotoDome` = equirect 구 | `L019-…webp` 4096×2048 |
| `panorama-video` | **과거** 재구성. 배경 평면 + `.glb` 건물 + 스프라이트시트 인물 + 스카이 마스크 + 동기 오디오 | `0_019_13K_fix.png` 7680×3840, `SKY_MASK-8.png` 4096×2048 |

- [검증] 두 씬은 `syncCameras()`가 `onViewMatrixChangedObservable`로 시점 미러링. 대조는 **CSS 한 줄** — 위 레이어 `div.scene-wrapper.comparingItem`에 `width:50%; overflow:hidden`, 경계에 `div.pano-slide-handle`. 라이브러리 없음. 투영은 equirect 하나. 카메라 fov 0.8rad ≈ 45.8°.
- [검증] **나침반 보정이 아예 없다.** `main.js`에 `DeviceMotionEvent.requestPermission`만 있고 `webkitCompassHeading`·`deviceorientationabsolute`·`compass`·`recenter` 문자열 0회. 절대 방위를 안 쓰고 `DeviceOrientationCamera`의 **상대 방위**만 쓴다 → 시작 시 폰이 향한 쪽이 정면. **방위 정합은 100% AR Clock이 떠받친다.** 센서 거부 시 `FreeCamera`로 조용히 강등, 데스크톱은 "Drag the mouse to view 360-degree panorama".
- [검증] **과거 이미지는 부분 밴드가 아니라 전체 360 재구성.** 하늘은 별도 마스크로 분리해 애니메이션. 남산 스팟의 "하늘 채운 부분 밴드"는 다른 트랙. 그들도 하늘만은 합성으로 메운다.

### 9.4 공유

- [검증] **합성 이미지 없음, Web Share API 없음, 워터마크 없음.** `navigator.share`·`toDataURL`·`toBlob`·`html2canvas` 전부 부재. 실체는 Facebook SDK + Twitter intent + Weibo intent 3버튼으로 **스팟 URL만** 전송.
- [검증] `og:image`가 스팟 이미지가 아니라 **전 스팟 공통 하늘 사진**(`SKY_BG-15-1920x960.jpg`). 공유 카드에 그 스팟 풍경이 안 뜬다. 셀피는 **앱 전용**(웹 코드에 `createSelfieSpot`이 있으나 FAQ는 앱 기능으로 명시).

### 9.5 기술 스택 / 성능

- [검증] WordPress + jQuery 3.4.1 + Bootstrap + slick + GSAP 3.3.4 + **Babylon.js 4.2** + p5.js 1.0.0 + video.js 8.16.1 + videojs-vr 2.0.0 + Adobe Typekit. 자체 호스팅(`/media/YYYY/MM/`), CDN은 cdnjs만. GA4 `G-NEG3N7XWW4` + Facebook SDK. **PWA 아님** — manifest·sw.js 전부 404.
- [검증] 번들 낭비: 포토돔 하나에 `ammo.js`+`cannon.js`+`Oimo.js`(물리엔진 3종)·`gltf_validator.js`·**`babylon.inspector.bundle.js`(디버그 인스펙터가 프로덕션에)** 전부 로드.
- [검증] **2025년 신규 지구에서 페이로드 붕괴.** `klc-002` 실측:

| 자산 | 크기 |
|---|---|
| `KowloonCity_L2_WalledCity_2025-final_v2-copy.jpg` | 29.1 MB |
| `KLC_location2_artwork_360_RT_2.jpg` | 37.1 MB |
| `KC2.glb` | 5.1 MB |
| `KC02A.wav` (무압축) | 6.1 MB |
| **합계** | **≈ 77 MB / 스팟** |

2021년 `ymt-jd-004`는 webp 2.4 MB. **webp → 무변환 JPG로 회귀**, 10배 악화. `forceOpaqueForKC3()` 같은 스팟별 하드코딩 예외도 남아 있다.

### 9.6 크레딧 / 라이선스

- [검증] 출처는 핫스팟 서술 끝 1줄: `Photo courtesy of Government Photo Store, Information Services Department` + `Learn more`. 파일명에도 출처(`…-from-HK-Museum-of-History.jpg`, `…-from-Government-Records-Service.jpg`, `…-from-University-of-Bristol-Library.jpg`).
- [검증] 제공기관 4곳 확인: 정부기록처(GRS), 정보서비스부 정부사진고(ISD), 홍콩역사박물관, 브리스톨대 도서관(Historical Photographs of China). **통합 크레딧 페이지 없음**(`/our-team/` 404).
- [검증] 이용약관은 전부 유보형("limited, non-exclusive, non-transferable … revocable license", 개인·비상업 1부 열람, 2차적 저작물 금지). 이미지 재사용 정책 없음.

### 9.7 사업 / 제도

- [검증] 여행사무서(Tourism Commission) 발주 + 홍콩城市大學 창의미디어학원(Jeffrey Shaw 팀) 제작. 출시 **2021-03-25, 13개**(Central 7 + TST 6) → 鯉魚門 2024-06 → 九龍城 5개 2025-09/10 → 46개. **지구 단위 연차 출하** 5년째.
- [검증] **이용자 수·다운로드·예산 어디에도 미공개.** info.gov.hk 보도자료, tourism.gov.hk, 사이트 미디어 페이지(2025-10 기사 10건) 모두 수치 없음. 공모 신청서에 "홍콩 실적"을 인용할 수 없다.
- [검증] 영화 연계 강화: `klc-002`는 《阿飛正傳》·《九龍城寨之圍城》(2024) 스틸 로드. 신작 로케 연계로 신규성을 만든다.

### 9.8 약점

1. 77 MB 페이로드 — 현장 LTE에서 사실상 불가. 웹판의 존재 이유를 스스로 깎는다.
2. 공유가 죽어 있다 — og:image 전 스팟 동일. SNS 확산 엔진 부재.
3. 검색·내 주변 없음 — 마커 없이 온 사람은 스팟을 못 찾는다.
4. 스팟 페이지 텍스트 0 — 3D를 안 켜면 아무 정보도 없다. SEO 제목 자동생성.
5. 출처가 3D 안 모달에 갇힘 — 통합 크레딧 페이지 없음.
6. 절대 방위 부재 — 원격 이용자는 시작 방위가 매번 다르다.
7. 프로덕션에 디버그 인스펙터·미사용 물리엔진 3종.
8. 쿠키 배너가 3D 화면 하단을 영구 가림.

### 9.9 서울 인 타임에 반영할 것

| # | 항목 | 태그 | 이유 | 기존 §7과의 관계 |
|---|---|---|---|---|
| 1 | **과거/현재 세로 와이프 슬라이더를 화면 안에** (`width:50%` + `overflow:hidden` + 핸들, 라이브러리 0) | [반영] | "대조의 순간"을 카메라 없이 화면만으로 성립시키는 가장 싼 수단. C안의 완성형 | §7.5가 `initSlideComparison`을 언급했으나 3D 씬 간 와이프라는 건 신규 |
| 2 | **상대 방위 + 물리 기준점.** 그들은 나침반 코드 0줄 | [반영·조건부] | 리뷰 #1 편각 문제는 마커가 있으면 풀 필요가 없는 문제. **단 Phase 0 마커 없는 조건에선 절대 방위가 여전히 필요** — 판정표의 2조건 그대로 | §7.3이 방향은 맞혔으나 "보정 없음"까진 미확인 |
| 3 | **og:image 스팟별 실제 이미지 + 합성 이미지 다운로드 + `navigator.share`** | [개선] | 그들의 최대 구멍. F5·F10이 성장 엔진이면 여기서 이긴다 | 신규 |
| 4 | **스팟당 배포 예산 상한 명문화**(예: ≤ 3 MB, equirect webp 4096×2048, 오디오 opus) | [개선] | 규격이 없어서 5년 만에 2.4 MB → 77 MB | §7.4가 webp 규격은 잡았으나 회귀 사실은 신규 |
| 5 | **핫스팟 방식 콘텐츠**(스팟당 3~5개, 각 사진 1장 + 서술 + 출처 1줄) | [반영] | 스팟 = 사진 1장이 아니라 "장면 + 이야기 N개" | 신규 |
| 6 | **출처 문구 `사진 제공: {소장처}` + 파일명에 소장처 각인** | [반영] | 리뷰 #7의 구체적 해법 | §1-8이 자리만 지적, 문구·파일명은 신규 |
| 7 | **스팟 페이지에 텍스트**(연도·지명·요약·출처를 3D 밖 HTML로) | [개선] | 검색·공유·접근성이 여기서 산다 | 신규 |
| 8 | **지도 옆에 검색 + 내 주변** | [개선] | 마커 없는 우리에겐 치명적 | §2의 반대편 교훈 |
| 9 | **스팟 좌표를 공공데이터로 개방**(서울 열린데이터광장) | [반영] | 공모 심사 "개방성" + 협의 명분 | 신규 |
| 10 | **지구 단위 연차 출하 + `hidden_spot`** | [반영] | `hidden_spot`은 2025년에야 도입된 후발 장치. 우리도 나중에 | §7.6 재확인 + 우선순위 하향 |
| 11 | 네이티브 앱을 본체로 | [무시] | PWA 전제를 뒤엎는다. 1번 웹 와이프로 대체 | §1-3 오인 정정 |
| 12 | 과거 전체 360 재구성(glb + 스프라이트 + 8K 플레이트) | [무시] | 스팟당 원가 수십 배. **하늘 마스크만 채택** | §6·§7.4 동일 결론 |
| 13 | Weibo/Twitter intent 3버튼 | [무시] | 한국은 카카오·인스타 | 신규 |
| 14 | 영화·드라마 로케 연계 축 | [개선] | 서울은 K-드라마 로케라는 더 강한 카드 | §7.2가 존재만 언급 |
