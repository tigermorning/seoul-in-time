# 기획·구현 검토 — 2026-09-10

> 킥오프 당일 산출물(PRD·MVP·LICENSE_GATE·PLATFORM_COMPARISON·SPOT_SCHEMA·스캐폴딩·정렬
> 프로토타입) 전체를 독립 리뷰어(critic 에이전트, Opus)가 훑은 결과. 15건. 심각도 순.
> 각 항목은 **구멍 / 왜 중요한가 / 최소 수정**. 처리 상태는 §2에 기록.

## 1. 발견 사항

### BLOCKER

**1. iOS 편각 이중 보정 — 문서와 코드 충돌**
PLATFORM_COMPARISON §2-A·§6은 iOS `webkitCompassHeading`을 "진북 기준"이라 적었는데
`heading.ts`의 `trueHeadingFromReading()`은 iOS 경로에도 편각(−8.5°)을 적용한다. 테스트가
그 동작을 고정시켜 놓음. iOS에서 상시 8.5° 오차 = 오차 예산(±15°)의 절반, `ALIGNED_TOLERANCE_DEG=4`의 2배.
→ 수정: 소스별 편각 적용 여부 분기 + **실기기 1회 측정으로 어느 쪽이 맞는지 확정**. Apple 문서
표현이 모호하므로 측정이 근거.

**2. 세로축 정렬 없음 — `pitch_deg`·`eye_height_m`은 죽은 필드**
`AlignScreen`은 오버레이를 세로 중앙 고정. `beta`/`gamma` 안 읽음. 폰 기울기가 바뀌어도 사진은
안 움직인다. 정렬은 2축 문제인데 반쪽만 구현.
→ 수정: `(beta − 90 − pitch) × pxPerDeg` 세로 오프셋 + `gamma`로 `rotate()`. 10줄.

**3. Android `360 − alpha`는 폰을 눕혔을 때만 맞는다**
카메라를 수평선에 겨누면 `beta≈90` — 오일러 분해의 특이점. `alpha`가 `gamma`와 뒤엉켜 튄다.
화면 회전 보정도 없음. Android 단말 다수인 Phase 0에서 방위값이 노이즈.
→ 수정: alpha/beta/gamma로 회전행렬 → 기기 −Z축(카메라 광축)을 수평면에 투영해 방위 산출.
순수 함수, 30줄, 테스트 가능.

**4. "정렬 성공률 70%·중앙값 30초"를 잴 수단이 저장소에 없다**
PRD §6·MVP §5.4가 판정 기준인데 타이머·성공 응답·세션 기록 전무. "성공"의 조작적 정의도 없음.
Phase 0의 유일한 판정 게이트이자 플랫폼 재검토 트리거.
→ 수정: [시작]/[맞았다]/[못 맞췄다] 3버튼 → `localStorage`에 `{spot, ms, trim, hfov, 결과}`
적재 + JSON 내보내기. 반나절. 질문 문구 고정한 프로토콜 1장.

**5. MVP 3곳 중 실제 스팟은 청계천 2건 — 검증 설계 붕괴**
MVP §2는 광화문(난이도 하한)·명동(GPS 최악)·청계천(다중 시점)을 서로 다른 조건 검증용으로
골랐다. 저장소엔 청계천만 2건. `historical[]`도 스팟당 1장이라 "3개 시점 대조"도 미충족.
→ 수정: 다음 콘텐츠 작업을 **광화문 1건**으로 고정 (서울역사아카이브 — 배지 확실).

### SHOULD-FIX

**6. 라이선스 자기모순 — §3-1을 데이터가 위반**
LICENSE_GATE §3-1 "공공누리 표시 없는 자료는 쓰지 않는다" ↔ §6 서울기록원 배지 없음 확인 ↔
스팟 JSON은 `KOGL-1` 확정 기록. 스키마에 "추론"과 "확인"을 구분할 필드 없음.
→ 수정: `license.determination: "declared"|"inferred"` 추가(추론이면 `evidence_quote` 필수),
또는 §3-1에 예외 명시. **서면 확인을 Phase 2 → 공개 배포 전으로 당김.** Pages 배포가 곧 공표.

**7. 출처표시가 앱에 한 글자도 안 뜬다**
`license.attribution`은 required인데 코드 어디서도 안 읽힘. 문구도 공공누리 권장형식과
달리 유형 표기·기관 URL 누락. 사진은 이미 `public/`에 커밋 → 배포 순간 무출처 이용.
→ 수정: `AlignScreen` 하단 attribution 1줄 + 공유 합성 이미지에 굽기. 문구 보강.

**8. 스키마가 정렬 코드에 필요한 것을 표현 못 함**
① `image.crop` 정의만 있고 코드 무시 → 엽서 테두리 그대로 오버레이. ② `hfov_deg`가 크롭
전/후 기준인지 미정의. ③ 세로 화각·광축 위치(principal point) 없음 — 정면 구도 아니면
`heading_deg` 자체가 모호. ④ `alignment.hints[].point` 채웠지만 코드가 안 씀.
→ 수정: "hfov는 크롭 후 기준, 크롭 중심 = 광축" 두 규칙 명문화. `crop`은 CSS로 즉시 적용.

**9. "정렬됨" 판정(±4°)이 센서 오차(±15°)보다 촘촘**
배지가 무작위로 켜진다. 사용자가 배지를 믿으면 안 맞은 상태에서 "성공" 응답 → 70% 오염.
→ 수정: 배지 기준을 오차 예산에 맞추거나(±10~15°), 배지 제거하고 성공 판정은 사용자 눈에.

**10. iOS 권한 체인이 `await`로 끊긴다**
`start()`가 `await requestOrientationPermission()` **다음에** `startRearCamera()` — Safari는
그 사이 transient activation 소진. `if (!v) return`이면 `phase='starting'` 영구 고착.
→ 수정: 두 Promise 동시 시작 또는 카메라 먼저. `!v` 분기에서 `setPhase('idle')`. 3줄.

**11. `object-cover`가 화각 계산 전제를 깬다**
영상을 잘라 채우므로 프레임 폭이 덮는 실제 화각 ≠ `cameraHfov`(65 고정). 매 세션 슬라이더로
손보정 → "중앙값 30초"와 충돌.
→ 수정: `track.getSettings()`로 종횡비 받아 크롭 비율 보정, 기기별 보정값 `localStorage` 1회 저장.

**12. 배포 첫 실행에서 깨질 지점**
① Pages Source를 GitHub Actions로 안 바꾸면 `deploy-pages@v4` 실패. ② `cancel-in-progress: true`는
Pages 배포 중 취소 시 잠길 수 있음(공식 권장 `false`). ③ 저장소명이 `seoul-in-time`이 아니면
`VITE_BASE` 불일치로 자산 404. ④ 저장소 아직 없음.
→ 수정: `cancel-in-progress: false`, README 배포 절차에 Pages Source 설정을 첫 push 전으로.

**13. README가 자기 자신과 어긋난다**
"코드 없음" ↔ "정렬 프로토타입 동작". 1964 사진 "아직 없음" ↔ 커밋됨. "draft 1건" ↔ 2건.
PLATFORM §6 데이터 위치 `public/spots/*.json` ↔ 실제 `spots/*.json`. Playwright·sharp 스크립트
확정 스택으로 적었으나 둘 다 없음.
→ 수정: 상태표·다음 할 일 정리, PLATFORM §6에 "미구현" 표시.

### NICE

**14. 검증기가 놓치는 규칙**
`image.file` 실존 여부, `crop.x+w≤1`, `evidence: metadata|statement`면 `evidence_quote` 필수,
`landmarks[].bearing_deg`와 `heading_deg` 정합. `src/types/spot.ts`는 손 미러링 사본.
→ 수정: 규칙 3개 추가(10줄). 타입은 `json-schema-to-typescript`로 생성해 `check`에 편입.

**15. 실사용자 베타의 전제가 비어 있다**
초상권 기준 미해결인데 인물 식별 가능한 사진을 `public/`에 커밋. 현장 테스트 10명 동의·개인정보
절차 없음. 분석 배제라 피드백 통로 없음.
→ 수정: (a) "인물이 화면 1/3 이상이면 보류" 한 줄 기준, (b) A4 동의문, (c) 자유서술 1칸 →
`localStorage`(4번과 같은 저장소).

## 2. 처리 상태

| # | 상태 | 비고 |
|---|---|---|
| 4 | ✅ 처리 | `src/lib/trials.ts` + `AlignScreen` 판정 버튼·기준선 질문 + 홈 `TrialPanel`(성공률·중앙값·내보내기) + [TEST_PROTOCOL.md](TEST_PROTOCOL.md). "맞았다"+"모르겠음"만 선택은 fail로 기록 |
| 15(c) | ✅ 일부 | 피드백 통로 = 기준선 질문 + `note`. 동의문 요지는 TEST_PROTOCOL §6 |
| 3 | ✅ 처리 | `cameraOrientationFromEuler()` — Z-X'-Y'' 회전행렬로 카메라 축(−Z)을 지구 좌표에 투영해 방위·피치·롤 산출. `beta≈90`에서 alpha/gamma 뒤엉킴 해소. beta/gamma 없으면 `360−alpha` 폴백. 테스트 10개 추가. 화면 회전은 앱이 portrait 고정이라 롤에만 영향 — 문서화 |
| 1·2 | ⬜ | 다음 순서. #2는 `subscribeCameraPose`가 이미 pitch/roll을 넘기므로 오버레이 오프셋만 남음 |
| 5~14 | ⬜ | |

## 3. 리뷰어 총평 (원문)

> Phase 0의 판정 게이트(70%/30초)를 **측정할 수단이 없고**, 측정 대상인 정렬은 **가로축만
> 구현**되어 있으며, iOS는 문서 기준으로 **상시 8.5° 오차**를 안고 있다. 라이선스 쪽은 조사
> 품질이 높지만, 자기 규칙(§3-1)을 자기 데이터가 어기는 상태이고 출처표시가 앱에 표시되지
> 않는다. 1·2·3·4번을 고치기 전에 스팟을 더 만드는 것은 MVP.md §5가 스스로 "최악 시나리오"라
> 부른 그 경로다.
