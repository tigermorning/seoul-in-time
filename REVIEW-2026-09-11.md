# 기획·진척 검토 — 2026-09-11

> 이틀치 산출물(26 커밋: 코드 9 / 문서 13 / 콘텐츠 4)을 독립 리뷰어(critic 에이전트, Opus)가
> PRD·MVP·TEST_PROTOCOL·LICENSE_GATE·BENCHMARK·BACKLOG·코드와 대조한 결과.
> 핵심 주장 4건(출처 링크 미렌더, Trial 편각 필드 부재, MVP.md 미수정 시각, 편각 기본 적용)은
> 메인 세션에서 코드로 재확인했다. 처리 상태는 §3에 기록.
>
> **판정: REVISE.** 조사·계측 품질은 높다. 문제는 지난 24시간의 작업이 MVP 범위 밖에서
> 진행됐고 그 피벗이 어느 문서에도 없다는 것, 그리고 Phase 0의 유일한 게이트(70% / 30초)에
> **해석 규칙이 없다**는 것.

## 1. 기획 품질

### A1. (치명) MVP가 코드에 의해 무효화됐는데 문서는 그대로 — 드리프트

| 문서 | 진술 | 실제 |
|---|---|---|
| MVP.md §3 "정렬 방식" | "**MVP는 파노라마를 만들지 않는다**" | `PanoScreen.tsx` + `panoScene.ts` (09-10 20:06) |
| MVP.md §4 | "❌ 360도 파노라마" | `spots/namsan-hoehyeon.json` equirect 3장 (09-11) |
| MVP.md §6 보류 1번 | 360 파노라마 | 09-11 커밋 1,232줄 전부 여기 |
| README.md "원칙" | "MVP에 없는 기능은 만들지 않는다" | 문구만 남고 위반 |
| BENCHMARK §6 | "MVP §3 기결정 **유지**" | 같은 문서 §8에서 B안 [x] 완료 |

MVP.md는 09-10 15:09 이후 한 줄도 수정되지 않았다. 피벗이라면 MVP §3·§4·§6을 고치고
"3개 스팟" 검증 설계를 다시 써야 했다. MVP §2의 검증 설계(광화문 = 정렬 하한 / 명동 = GPS
최악 / 청계천 = 다중 시점)는 **여전히 0/3 충족**(리뷰 09-10 #5 BLOCKER 미해결)이고, 그
상태에서 네 번째 스팟이 추가됐다.

### A2. (치명) A/B/C 세 방식에 선택 규칙이 없다 — Phase 0 판정 불가

- BENCHMARK §7.3이 3안 비교표를 만들었지만 **무엇을 재서 어느 방식으로 간다**는 규칙이 없다.
- TEST_PROTOCOL은 타이머 시작을 "[카메라 켜기] 탭"으로 고정. B·C 모드엔 그 버튼이 없다
  (PanoScreen은 [시작]). **측정 절차가 구현과 어긋난 상태**이고 BACKLOG 6번이 이를 알고도 방치.
- 표본 `n ≥ 10`인데 방식 3 × 마커 조건 2 = 셀당 1.7명. **어떤 판정도 못 나온다.**
- BENCHMARK §3이 요구한 PRD §6의 2조건 판정표 교체는 PRD.md에 미반영.

즉 계측 코드(`trials.ts`)는 생겼는데 **해석 규칙이 없다.** BENCHMARK §3이 스스로 적은
경고이며 하루가 지나도록 그대로다.

### A3. (중대) 합성 파노라마 vs PRD 비목표 5

PRD 비목표 5 / README: "복원·변형 안 함. 원본의 사실성이 신뢰 기반." 그러나 1974 equirect는
8장 스티칭 + 밝기 보정이고, **사진 없는 방향은 하늘·지면 톤 그라데이션으로 채웠다**
(PANORAMA_SOURCES.md 「합성 이미지」). 반면 PanoScreen 안내문은 "사진이 없는 방향은 **어둡게
비어 있습니다**"라고 말한다. 실제로는 그럴듯한 하늘이라 **사용자가 합성 영역을 구분할 수 없다.**
"합성 허용 범위·표기 규칙"이 어느 문서에도 없다. 위촉 일러스트 허용 여부도 미정(BENCHMARK §8).

### A4. (중대) 라이선스 게이트: 규칙과 데이터의 자기모순이 확대됨

- LICENSE_GATE §3-1 "공공누리 표시 없는 자료는 쓰지 않는다" ↔ 서울기록원 3건(청계천 2 +
  남산 1974)이 `evidence: "metadata"`로 `type: KOGL-1` 확정 기록. 리뷰 09-10 #6이 제안한
  `license.determination` 필드 미적용. 위반 건수 2 → 3.
- **출처표시 미이행.** `license.attribution` 전문과 `source.url`(원본 보기)은 코드 어디서도
  렌더되지 않는다(`grep source.url src` = 0건). PanoScreen·WindowScreen이 `org · archive_id ·
  KOGL-1` 한 줄을 띄울 뿐이고 AlignScreen은 그것도 없다. **GitHub Pages 배포는 이미 공표**이므로
  지금 상태가 공공누리 1유형의 유일한 의무(출처표시)를 어긴다. 리뷰 09-10 #7 미해결.

### A5. (중대) PM 관점에서 통째로 빠진 것

| 결손 | 근거 |
|---|---|
| 사용자 리서치 0 | PRD §3 페르소나 4개 전부 추정. 인터뷰·설문 기록 없음 |
| 발견/유입 경로 | 딥링크 없음 — `App.tsx`가 `useState` 라우팅. QR·SNS 공유(F5·F10) 둘 다 전제 미충족 |
| 콘텐츠 원가 실측 | PRD §8.3 "스팟당 수 시간~수십 시간". **남산 1건에 실제 몇 시간 들었는지 기록이 없다.** Phase 1(30곳) 추정의 유일한 근거를 지금 안 재고 있다 |
| 현장 테스트 10명 모집 계획 | 누구를·어디서·언제가 없음. 절차(TEST_PROTOCOL)만 존재 |
| 접근성 | 어느 문서에도 없음. (오프라인은 MVP §4에서 의도적 제외 — 정당) |

PRD 자체의 내부 일관성(문제 → 사용자 → 핵심 경험 → 기능 → 기준 → 비목표)은 양호하고, §4
"정렬의 순간" 판정 기준은 기능 취사선택에 실제로 쓸 수 있다. 문제는 일관성이 아니라
**결정이 갱신되는 속도를 문서가 못 따라간다**는 것이다(BENCHMARK §8 미적용 12건 중 문서
항목 적용 0건).

## 2. 진척도

### B1. Phase 0 / MVP DoD 대비

| 항목 | 상태 | 근거 |
|---|---|---|
| §5.1 게이트 3건 | 부분 | 해상도 2건 ✅ / **초상권 기준 미정** |
| §5.2 스팟 3곳 데이터 | 미착수 | 광화문 0, 명동 0, 청계천 2건 `draft`·`confidence: rough` |
| §5.2 현장 답사 | **0회** | 전 스팟 `surveyed_at: null` |
| F1 스팟 발견 | 완료 | `SpotMap.tsx`. 단 `demo-pano` 격자가 배포 목록에 노출 |
| F2 정렬 가이드 | 부분 | `guide.instruction` + 방위 배지. `stand_here_image` 전부 `null` |
| F3 대조 뷰 | 부분 | A모드만 `reveal` 슬라이더. B·C는 현재 풍경이 화면 밖이라 앱 안에 "대조"가 없음 |
| F4 메타 카드 | **미착수** | 원본 링크·attribution 전문 렌더 0곳 |
| F5 공유 | **미착수** | `App.tsx` placeholder |
| F10 원격 모드 | **미착수** | 전 스팟 `present: null` |
| §5.3 iOS/Android 실기기 | **미검증** | B 데모 격자의 상대 반응만 확인 |
| §5.4 성공률·중앙값 | 계측 ✅ / 데이터 n=0 | `trials.ts` |
| §5.5 위생 | 부분 | README "코드 없음" ↔ "프로토타입 3종" (리뷰 #13). `deploy.yml` `cancel-in-progress: true` (리뷰 #12) |

### B2. 실기기 미검증 항목과 부호 위험 — 가장 큰 단일 리스크

- **절대 방위 0회 검증.** PanoScreen·AlignScreen이 편각 −8.5°를 **기본 적용**(`useState(true)`).
  iOS `webkitCompassHeading`이 진북이면 상시 8.5° 오차. 리뷰 09-10 #1 BLOCKER 미해결.
- **`Trial` 레코드에 편각 토글 상태와 마커 조건이 없다**(`trials.ts` `Trial` 타입). 지금 상태로
  10명을 모으면 **iOS 데이터는 사후 보정이 불가능해 폐기해야 한다.**
- `panoScene.ts`의 `rotation.y = (-90 - heading)`, `rotation.set(pitch, -heading, -roll)` 미검증.
  부호가 틀리면 실패 원인이 플랫폼으로 오독되고 PLATFORM_COMPARISON §5의 "뒤집는 조건"이
  발동한다 — MVP가 스스로 "최악 시나리오"라 부른 경로.
- 남산 촬영 지점(37.5541, 126.9857)은 케이블카 선로 부근. **사람이 설 수 있는지 미확인.**
  접근 불가면 스팟이 성립하지 않는다.

### B3. 노력 배분 — 크리티컬 패스를 벗어남

09-11 두 커밋 1,286줄이 전부 MVP 범위 밖(남산 파노라마). 같은 기간 MVP 크리티컬 패스
(광화문 스팟 1건, 실기기 1회 검증, F4·F5) 진척은 0. 리뷰 09-10 15건 중 처리된 것은
#2·#3·#4뿐이고 나머지 12건(#1 포함)은 그대로인데 새 기능이 둘(B·C 모드) 늘었다.

**다음에 할 3가지**
1. **10분 절대 방위 검증 + `Trial`에 `declination_applied` / `marker` 필드 추가.** 계측기를
   고치기 전의 모든 현장 데이터는 버려진다. 최우선.
2. **판정 규칙을 먼저 쓴다.** A/B/C 중 Phase 0 본선 **하나**를 고르는 기준 + PRD §6을
   2조건표로 교체 + TEST_PROTOCOL을 B/C 문구로 갱신. n=10으로 3방식은 못 잰다.
3. **남산 답사 1회** — 접근 가능 여부·실측 방위 확인. **이 스팟에 든 실제 시간을 기록**
   (Phase 1 로드맵의 유일한 실측 근거).

**중단/연기할 3가지**
1. `--width 8192`, 시대 선택 탭, 추가 파노라마 스팟 — **폰 1회 검증 전엔 전부 중단.**
2. A 카메라 오버레이 잔여 수정(리뷰 #8·#9·#11) — 방식 결정 전에는 사장될 수 있는 코드.
3. 광화문·명동 콘텐츠 제작과 BENCHMARK §8 문서 대량 재작성. 단 **출처표시 렌더링(리뷰 #7)은
   예외로 즉시 처리** — 배포본이 라이선스를 위반 중이다.

## 3. 처리 상태

- [ ] A1 MVP.md 피벗 반영 (§3·§4·§6)
- [ ] A2 Phase 0 판정 규칙 + PRD §6 2조건표 + TEST_PROTOCOL B/C 문구
- [ ] A3 합성 허용 범위·표기 규칙 문서화 + PanoScreen 안내문 수정
- [x] A4 attribution·원본 링크 렌더 (리뷰 09-10 #7) → 09-12 `PhotoCredit.tsx`
- [ ] A4 `license.determination` 필드 (리뷰 09-10 #6)
- [ ] B2 절대 방위 검증 (BACKLOG 1번) + `Trial` 필드 추가
