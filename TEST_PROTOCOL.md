# Phase 0 현장 테스트 프로토콜

> PRD §6 "정렬 성공률 70%+, 정렬까지 중앙값 30초 이하"를 **같은 방식으로** 재는 절차.
> 이 문서와 앱의 기록 기능이 한 쌍이다. 절차를 바꾸면 이 문서를 먼저 고친다.

## 1. 측정 단위 — 시도(Trial)

**참가자 1명 × 스팟 1곳 = 시도 1건.** 앱이 자동 기록:

| 필드 | 뜻 |
|---|---|
| `ms` | [카메라 켜기] 탭 → [맞았다]/[못 맞췄다] 탭까지 |
| `result` | `success` / `fail` / `abandon`(뒤로가기·탭 이탈) |
| `anchors` | 참가자가 "겹쳐 보였다"고 고른 것: 능선·도로·건물·물길·기타·모르겠음 |
| `trim_deg`, `camera_hfov`, `opacity` | 판정 순간 슬라이더 값 — 센서가 얼마나 틀렸는지의 간접 지표 |
| `final_delta_deg` | 판정 순간 나침반 기준 목표와의 차이 |
| `sensor` | 나침반 권한 상태 (iOS 거부 등) |

## 2. "성공"의 정의 — 고정

> **참가자 본인이** 옛 사진 속 고정선(능선·도로축·건물 윤곽·물길) 중 하나 이상이 현재
> 풍경과 겹쳐 보인다고 판단해 [맞았다]를 누르고, 이어지는 질문에서 **"모르겠음" 외의
> 항목을 하나 이상 고른 경우.**

- [맞았다] + 모르겠음만 선택 → 앱이 `fail`로 기록한다. 배지를 믿고 눌렀을 가능성.
- 관찰자는 판정에 개입하지 않는다. 힌트 금지. 질문에 "화면 안내대로 해보세요"만.
- 관찰자 판단이 참가자와 다르면 `note`에 적는다. 수치는 참가자 기준.

## 3. 절차 (참가자 1명당 10분)

1. **동의** — A4 1장 (§6). 서명 전엔 시작 안 함.
2. 앱 링크 열기. 홈에서 스팟 선택. 안내 화면을 **참가자가 직접 읽게** 한다.
3. 참가자가 [시작하기] → [카메라 켜기]. **여기서 타이머 시작.** 관찰자는 시계 안 봐도 됨.
4. 참가자가 정렬 시도. 슬라이더 만져도 됨. 관찰자 무개입.
5. 참가자가 [맞았다] 또는 [못 맞췄다]. **타이머 종료.**
6. 앱이 묻는 "무엇이 겹쳐 보였나요?"에 참가자가 답함.
7. 관찰자 질문 1개, 문구 고정: **"방금 맞출 때 제일 헷갈린 게 뭐였어요?"** → `note`에 요약.
8. 다음 스팟 또는 종료.

이탈 규칙: 3분 넘으면 관찰자가 "그만하셔도 돼요"라고 말한다. 참가자가 [못 맞췄다]를 누르게 한다
(관찰자가 대신 누르지 않음). 뒤로가기로 나가면 `abandon`.

## 4. 데이터 수거

홈 화면 하단 "실험 기록" → [내보내기]. JSON이 공유 시트로 나감(iOS) 또는 클립보드로
복사됨. 관찰자 폰으로 보내 `field-tests/YYYY-MM-DD-<참가자번호>.json`에 저장.
**참가자 이름은 어디에도 안 적는다.** 번호만.

## 5. 판정

`n ≥ 10` 모이면:
- 성공률 = `success` / 전체 (abandon 포함)
- 중앙값 = `success` 시도의 `ms` 중앙값

| 결과 | 조치 |
|---|---|
| 성공률 ≥ 70% **and** 중앙값 ≤ 30초 | Phase 0 통과. 스팟 확장 |
| 성공률 ≥ 70%, 중앙값 > 30초 | UX 문제. 슬라이더·안내 손보고 재측정 |
| 성공률 < 70% | **플랫폼 재검토** (PLATFORM_COMPARISON §5 뒤집는 조건). 스팟 더 만들지 않음 |

`anchors` 분포도 본다 — "능선"만 성공하고 "건물"은 실패하면 스팟 선정 기준이 바뀐다.

## 6. 동의문 (A4, 요지)

- 목적: 옛 사진 정렬 앱 시험. 결과는 앱 개선에만 사용.
- 기록되는 것: 소요시간, 성공 여부, 슬라이더 값, 나침반 값, 기기 종류. **이름·사진·위치 이력 기록 안 함.**
- 언제든 중단 가능, 불이익 없음.
- 서명 / 날짜 / 참가자 번호
